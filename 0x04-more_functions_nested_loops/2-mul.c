#include "main.h"

/**
 * mul - Multiply two integers
 * @a: the first int to multiply
 * @b: the second int to multiply
 * Return: The result of the multiplication between the two int
 */

int mul(int a, int b)
{
	return (a * b);
}
